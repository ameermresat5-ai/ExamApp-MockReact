# Data source: "json" or "postgres"
DATA_SOURCE=postgres

# Local PostgreSQL running through Docker
DATABASE_URL=postgresql://ameer:examapp_local_password@localhost:5433/examapp

# Set true only when the remote provider requires SSL
DB_SSL=false

PORT=5000
